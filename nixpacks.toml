# Railway / Nixpacks build config
# Forces a clean npm ci (not cached) so postinstall patches always run

[phases.install]
cmds = ["npm ci --omit=dev"]

[phases.build]
cmds = ["node scripts/patch-baileys.js"]

[start]
cmd = "node web/server.js"
